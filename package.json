{
  "name": "picbooth-by-kj",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first photo booth for any event, with Mac and iPad editions",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && python3 -m py_compile scripts/composite.py"
  },
  "engines": {
    "node": ">=18"
  }
}
